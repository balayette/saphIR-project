fun main() int {
        let int a = 0;
        let int b = 0;
        let int c = 0;

        for (let int z = 0; a != 10; a = a + 1)
                c = c + b;
                a = b * 2;
        rof

        return c;
}
