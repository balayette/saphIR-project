#include "libc.jit"

fun mult(int a, int b) int {
        int ret = 0;
        for (int i = 1; i != b + 1; i = i + 1)
                ret = ret + a;
        rof
        return ret;
}

fun fact(int n) int {
        int ret = 1;
        for (int i = 1; i != n + 1; i = i + 1)
                ret = mult(ret, i);
        rof
        return ret;
}

fun main(int a) int {
        for (int i = 0; i != a; i = i + 1)
                printf("%d\n", fact(i));
        rof

        return a;
}
